#!/usr/bin/env sh
if command -v bun >/dev/null 2>&1; then
  exec bun "$(dirname "$0")/bin.bun.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node "$(dirname "$0")/bin.node.js" "$@"
else
  echo "Error: need bun or node in PATH" >&2
  exit 1
fi